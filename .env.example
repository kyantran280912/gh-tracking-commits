# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
GITHUB_TOKEN=ghp_your_github_token_here

# Telegram Bot Token
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram Chat ID
# Get by sending a message to your bot, then visit:
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
TELEGRAM_CHAT_ID=123456789

# GitHub Repositories to track (comma-separated)
# Supports multiple formats:
# - owner/repo (default branch)
# - owner/repo:branch (specific branch)
# - https://github.com/owner/repo (full URL)
# - https://github.com/owner/repo/tree/branch (full URL with branch)
#
# Examples:
# GITHUB_REPOS=facebook/react,microsoft/typescript:main,vercel/next.js
# GITHUB_REPOS=https://github.com/facebook/react,https://github.com/vercel/next.js/tree/canary
#
GITHUB_REPOS=facebook/react,microsoft/typescript,vercel/next.js

# Check interval in hours (optional, default: 3)
CHECK_INTERVAL_HOURS=3

# Database file path (optional, default: ./db.json)
# Only used if DATABASE_URL is not provided (fallback to lowdb)
DB_PATH=./db.json

# PostgreSQL Database URL (required for dashboard)
# If provided, uses PostgreSQL instead of lowdb for storage
# Examples:
# - Local: postgresql://username:password@localhost:5432/database_name
# - Neon: postgresql://user:pass@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://username:password@localhost:5432/github_tracker

# ============================================
# DASHBOARD API SERVER CONFIG
# ============================================

# API Server Port (default: 3001)
PORT=3001

# Node Environment
NODE_ENV=development

# JWT Secret (REQUIRED - minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# JWT Expiration (default: 7d)
JWT_EXPIRES_IN=7d

# CORS Origins (comma-separated, default: http://localhost:3000)
CORS_ORIGIN=http://localhost:3000

# ============================================
# NEXT.JS DASHBOARD CONFIG
# ============================================

# API URL for Next.js frontend
NEXT_PUBLIC_API_URL=http://localhost:3001

# NextAuth URL (for production deployment)
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (REQUIRED - minimum 32 characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-change-this-in-production-min-32-chars
